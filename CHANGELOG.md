# 변경 이력 (Changelog)

photoKam의 버전별 변경 사항입니다. 최신이 위에 옵니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/) 스타일을 느슨하게 따릅니다.

---

## v0.7.64 — HDR 동영상 썸네일 색감 수정(톤매핑)

### 수정
- 삼성 갤럭시 등 **HDR10+/HLG 동영상**의 썸네일이 "물빠진 색"으로 나오던 문제. HDR(PQ=smpte2084 / HLG=arib-std-b67)을 감지해 프레임을 **HDR→SDR 톤매핑**(zscale 선형광 + tonemap=hable, BT.709로 복원)한 뒤 캡처하도록 수정. SDR 동영상은 기존대로. 톤맵 실패 시 톤맵 없이 폴백.
- ※ 적용하려면 설정 → 저장소 → "동영상만 재생성"을 다시 실행하세요.

## v0.7.63 — 동영상 썸네일만 재생성

### 추가
- 설정 → 저장소에 **"동영상만 재생성"** 버튼. 전체 재스캔 없이 동영상 썸네일만 다시 만든다(0.7.62 채도 개선 반영용). 진행률은 상태바에 표시, 끝나면 자동 새로고침.
- thumb:// 응답이 immutable 캐시라 같은 URL이면 옛 이미지가 보이던 문제 → 썸네일 캐시 에폭을 도입해 재생성 후 모든 thumb URL을 한 번 무효화.

## v0.7.62 — 동영상 썸네일 채도 저하 수정

### 수정
- 동영상 썸네일이 사진보다 채도가 낮게 보이던 문제. 원인은 ffmpeg가 프레임을 **직접 손실 WebP(크로마 4:2:0 서브샘플링)** 로 인코딩한 것. 이제 프레임은 **PNG(무손실)** 로만 뽑고 사진과 동일한 `image` 크레이트(무손실 WebP)로 저장해 색감을 통일.
- ※ 기존에 만들어진 동영상 썸네일은 캐시에 남아 있으므로, 설정 → 썸네일 캐시 비우기 후 다시 스캔하면 새 색감으로 재생성됩니다.

## v0.7.61 — 기준 비교(Reference Compare) (Phase 2-3, Phase 2 완결)

### 추가
- 비교 뷰에 **기준 비교** 모드: 1장을 "기준"으로 고정하고 나머지를 **후보로 ←/→ 교체**하며 1:1 비교(줌/팬 동기화로 같은 영역을 같은 배율로). "기준으로" 버튼으로 현재 후보를 기준으로 승격. 각 칸에 기준/후보 라벨.
- 비교 버튼을 2장 이상이면 노출(상한 4장 제거) — 그리드 비교는 최대 4장, 기준 비교는 선택 전체를 후보로 순환.

## v0.7.60 — 지도/크게보기 불안정 수정 마무리(진단 로그 제거)

### 정리
- 0.7.59에서 원인 확정·해결(별도 창+원격 iframe 제거, Leaflet 통일)됨을 로그로 확인하여, 임시 진단 로그(`dlog`/`log_debug`)를 전부 제거. 기능 변화 없음. (`docs/bug-map-instability.md` 종료)

## v0.7.59 — 지도 안정화(Leaflet 통일) + 별도 창 제거 + 진단 로그

### 수정
- 인스펙터·모달 지도를 원격 OSM iframe 임베드 → **Leaflet(클라이언트 타일)**로 교체. WebView2에서 흰/빈 화면으로 안 뜨던 문제 대응(지도 모드와 동일 방식).
- 지도 **별도 창** 기능 제거(흰 창·고아 창의 근원). 지도는 인앱 레이어/패널만.

### 진단(임시)
- 크게보기 빈 화면(재시작 후 더블클릭 시) 원인 추적용 로그 추가(`%TEMP%\photokam-debug.log`).

## v0.7.58 — 크게보기 GPS "위치보기" 버튼 제거

### 변경
- 크게보기 인스펙터는 지도가 패널에 기본으로 보이므로 GPS 줄의 "위치보기" 버튼 제거(좌표만 표시). 상세 패널(그리드 우측)은 지도가 기본 표시가 아니라 버튼 유지.

## v0.7.57 — 지도 모드(전체 사진 위치)

### 추가
- **지도 모드**: 상단 네비에 🗺 추가. GPS 좌표가 있는 모든 사진을 큰 인터랙티브 지도(Leaflet + OSM 타일)에 핀으로 표시하고, **핀 클릭 → 크게보기**로 그 사진을 연다. 위치로 사진을 탐색하는 방식. (온라인 타일; 핀은 의존성 문제 없는 벡터 마커.)
- 백엔드 `list_gps_photos`(GPS 있는 사진 목록).

## v0.7.56 — 인스펙터 지도 크게 보기

### 추가
- 인스펙터(우측 패널) 지도 섹션에 **⤢ 크게** 버튼 — 클릭하면 큰 지도 모달(이동/별도 창 토글 포함)로 확대. 한 장의 위치를 크게 확인.

## v0.7.55 — 크게보기 단일 우측 인스펙터로 통합

### 변경
- 좌/우로 흩어졌던 분석 정보를 **우측 인스펙터 패널 하나**로 통합(Lightroom 방식). 위에서부터 **히스토그램(고정 높이) → EXIF·파일 정보(스크롤) → 지도(고정 높이, GPS 있을 때) → 별점/플래그(하단 고정)**. 좌측 패널 제거.
- 지도·히스토그램이 **고정 높이**라 세로로 길쭉해지던 문제 해결. 세로사진의 좌우 여백 한쪽을 패널이 활용해 화면을 더 효율적으로 사용.
- 토글: `Tab` 또는 `H`, 툴바 `▤`. GPS "위치보기"는 패널을 열어 지도를 보여줌. (떠다니던 히스토그램 버튼 `📊` 제거.)

## v0.7.54 — 크게보기 좌측 분석 패널(히스토그램 + 지도)

### 변경
- 크게보기에 **좌측 분석 패널** 추가: 위에는 **히스토그램**, 아래에는 **지도**(현재 사진 GPS). `📊` 버튼(단축키 `h`)으로 토글. GPS "위치보기"도 이 패널을 연다.
- 기존 떠다니던(floating) 히스토그램과 지도 모달은 크게보기에서 이 패널로 통합. 지도는 패널 하단에 항상 보이고, GPS가 없으면 "위치 정보 없음" 표시.
- (상세 패널의 히스토그램·지도 모달은 그대로 유지.)

## v0.7.53 — 지도 별도 창 ↔ 레이어 토글

### 추가
- 인앱 지도에 **별도 창으로 빼기**(⧉ 별도 창) 버튼 추가 — OS 레벨 독립 창으로 빠져 이동/리사이즈/다른 모니터 배치 가능(Tauri 백엔드가 OSM 임베드 창 생성).
- 이미 별도 창으로 빠져 있으면 **레이어로 넣기**(▣) 버튼으로 다시 인앱 모달로 복귀. 별도 창 포커스도 가능.

## v0.7.52 — 인앱 지도(GPS 위치보기)

### 추가
- **인앱 지도**: GPS "위치보기"를 누르면 외부 브라우저 대신 앱 안에서 지도(OpenStreetMap 임베드)로 위치를 표시(크게보기·상세 패널 공용). 모달 안에 구글맵/OSM 외부 링크와 좌표도 함께 제공. 온라인 옵션이라 인터넷이 없으면 지도 영역만 비고 좌표·링크는 동작. `Esc`로 닫기.

## v0.7.51 — 상세 패널 별점 박스 + 모서리 더 둥글게

### 개선
- 사진목록 우측 상세 패널의 별점/플래그도 크게보기처럼 둥근 사각형 박스로 감쌈.
- 별점/플래그 박스(상세 패널·Pro 사이드패널)의 모서리를 조금 더 둥글게(12px).

## v0.7.50 — 크게보기 별점/플래그 컨트롤 시각 강조

### 개선
- 크게보기 별점/플래그 컨트롤을 둥근 사각형으로 감싸 또렷하게 표시. Pro 사이드패널 하단 컨트롤은 테두리 박스로 감싸고, 비-Pro 상단 알약은 테두리·그림자·블러를 더해 어떤 사진 위에서도 잘 보이게 함.

## v0.7.49 — Pro 패널 전체 정보 + 별점 하단 배치 + GPS 지도 열기

### 개선
- **Pro 사이드패널 전체 정보**: 크게보기 Pro 패널이 기본 항목만 보여주던 것을 상세 패널과 동일한 전체 항목(크기·형식·35mm 환산·노출보정·측광·화이트밸런스·플래시·경로·수정일·인덱싱 등)으로 확장. 플로팅 정보(`i`) 패널도 동일.
- **별점/플래그 하단 배치**: Pro 사이드패널에서 별점/플래그를 정보(GPS 등) 위가 아니라 **맨 하단**으로 이동(상단 구분선).

### 추가
- **GPS 지도 열기**: GPS 좌표 옆 "위치보기" → 기본 브라우저에서 지도(구글맵)로 표시(크게보기·상세 패널 공용). 오프라인 우선 원칙에 따라 1차는 외부 링크.

## v0.7.48 — 히스토그램 이동/크기 조절 + 크게보기 툴바 가시성·닫기 버튼 구분

### 개선
- **히스토그램 이동·크기 조절**(크게보기): 헤더를 잡아 드래그하면 위치 이동, 우하단 핸들로 크기 조절. 상세 패널의 히스토그램은 그대로 인라인 고정.
- **툴바 버튼 가시성**: 크게보기 상단 아이콘이 확대된 사진 위에 떠 있을 때 잘 안 보이던 문제. 테마와 무관하게 불투명 다크 크롬으로 고정하고, 선택(on) 상태를 반투명 대신 강조색으로 꽉 채워 어떤 사진 위에서도 보이게 함.
- **닫기 버튼 혼동 방지**: 창닫기 ✕가 별점/플래그 줄의 탈락 ✕와 같은 선상에 있어 헷갈리던 문제. 별점/플래그 줄을 한 줄 아래로 내리고, 닫기 버튼은 간격+빨강 호버로 시각적으로 분리.

## v0.7.47 — 히스토그램 다듬기(반투명 합성 + 클리핑 표시)

### 개선
- RGB 채널을 더 낮은 불투명도(≈0.5)의 가산 합성으로 그려, 다른 앱처럼 채널이 층층이 비치고 겹침이 노랑·청록·자홍·흰색으로 보이게 조정.
- **클리핑 표시**: 양끝(0=암부, 255=명부)에 픽셀이 몰리면 히스토그램 코너에 경고 삼각형(비율이 클수록 밝게). 과노출 빗금과 짝이 되는 정보.

## v0.7.46 — 히스토그램(RGB/휘도) (Phase 2-4)

### 추가
- **히스토그램**: 크게보기 툴바 `📊`(단축키 `h`)와 상세 패널의 접이식 "히스토그램" 섹션. 원본을 512px로 축소 분석(제브라와 동일하게 crossOrigin+CORS로 캔버스 오염 회피)해 256-bin RGB(가산 합성) / 휘도 분포를 표시. 양끝(0·255) 스파이크는 정규화 기준에서 제외해 본문 가독성 확보. 상세 패널은 펼칠 때만 원본을 로드(그리드 탐색 부하 회피).
- 단축키 시트 "크게 보기"에 `H`(히스토그램)·`I`(정보)·`Tab`(Pro 레이아웃) 항목 추가.

## v0.7.45 — 사진 거쳐 동영상 복귀 지연 **근본 해결** + 진단 코드 정리

### 수정
- 동영상→사진→동영상으로 돌아올 때만 7~12초씩 멈추던 문제를 **확정 해결**. 근본 원인은 WebView2가 **커스텀 스킴(photo://) 응답을 직렬화**해, 8MB 사진 원본 응답이 큐를 점유하는 동안 바로 뒤따르는 IPC 명령(재생 준비) 응답까지 밀린 것. 큰 사진 원본을 photo:// 대신 **Tauri asset 프로토콜**로 로드하도록 전환해 해결(재생 준비 CALL→완료가 7.9초→11ms로). 라이트박스 본 이미지 표시를 asset 기반으로 변경.
- 이웃 원본 프리로드는 비활성화 유지(photo:// 프리로드가 같은 큐를 점유해 동영상 진입을 지연시켰던 원인). 본 이미지는 asset로 즉시 로드되고 흐린 썸네일이 깔려 prev/next는 충분히 빠름.

### 정리
- 0.7.41~0.7.44에서 원인 추적에 쓰던 임시 진단 코드 일괄 제거: 프론트 `dlog`/하트비트/미디어 이벤트 로그, 백엔드 `dbg_file`·`log_debug` 명령·`transcode_video` 프로브.

## v0.7.40 — 프로토콜 비동기화(사진 거쳐 동영상 복귀 지연 수정)

### 수정
- 사진→동영상 복귀 시 7~11초 지연되던 근본 원인 수정. 커스텀 프로토콜(thumb/photo/video)이 파일 전체를 **동기적으로 읽어 메인 스레드를 막아** 뒤따르는 명령(상세 로드·재생 준비)이 지연되던 것을, 핸들러를 **비동기(별도 스레드)** 로 전환해 해결. 로그 진단(prepare_video 1ms인데 결과 수신 11초)으로 확인.

## v0.7.39 — 동영상 항상 새로 로드(캐시 재사용 방지)

### 수정
- 사진을 거쳐 동영상으로 돌아올 때 느려지던 문제. 캐시(부분 evict/재검증)를 거치는 대신 매 재생마다 고유 토큰 + 플레이어 재마운트로 **항상 새로 로드**(더블클릭과 동일)하게 변경.

## v0.7.38 — 사진 거쳐 동영상 복귀 시 느려짐 완화

### 수정
- 동영상→사진→동영상으로 돌아올 때만 느리던 문제. 사진을 보며 잡힌 큰 디코드 이미지들이 메모리를 점유해 동영상 캐시를 밀어내던 것 → 동영상 진입 시 보관 이미지를 비워 메모리를 양보하도록 수정.

## v0.7.37 — 동영상 재생 중 이미지 프리로드 중단

### 수정
- 동영상을 보는 동안 이웃 이미지 원본 프리로드를 멈춰, 무거운 원본 로드가 동영상 스트리밍과 디스크를 다투어 재생 시작이 느려지던 문제 완화(특히 필름스트립으로 넘겨 동영상에 진입할 때).

## v0.7.36 — 동영상 준비 단순화(리먹스 제거)

### 변경
- asset 프로토콜이 Range를 효율적으로 처리하므로, 호환 코덱의 mp4/mov는 **faststart 리먹스 없이 원본을 바로 재생**하도록 단순화. 영상마다 리먹스 비용이 사라져 필름스트립으로 넘길 때도 빠름.

## v0.7.35 — 동영상 재생을 asset 프로토콜로 전환(속도)

### 변경
- 동영상 재생을 커스텀 video:// 프로토콜에서 **Tauri 내장 asset 프로토콜**(파일 스트리밍 최적화)로 전환. 재생할 실제 경로(원본/리먹스본/변환본)를 받아 asset URL로 재생 → 대용량 동영상 첫 로딩이 네이티브 플레이어에 더 가깝게 빨라짐.

## v0.7.34 — 동영상 스트리밍 청크 개선

### 수정
- 동영상 스트리밍 청크 크기 4MB → 16MB로 확대(왕복 감소), Range 없는 요청도 첫 청크만 보내 대용량 파일 전체를 메모리로 읽지 않도록 변경. faststart와 함께 첫 재생 시작을 단축.

## v0.7.33 — 액션캠/드론 mp4 재생 불가 회귀 수정

### 수정
- faststart 리먹스 시 데이터 스트림(드론·액션캠 텔레메트리 등)까지 복사하려다 mp4 먹싱이 실패해 일부 mp4(예: DJI)가 "재생할 수 없습니다"로 나오던 회귀 수정. 리먹스를 비디오·오디오만 복사하도록 변경 + 실패 시 원본 재생 폴백.

## v0.7.32 — 대용량 동영상 첫 로딩 속도 개선(faststart)

### 수정
- 대용량 동영상이 첫 재생 시작에 10초 이상 걸리던 문제. 원인은 재생정보(moov)가 파일 끝에 있어(faststart 아님) 시작 전에 파일을 많이 읽어야 했던 것. 해당 동영상을 **코덱은 그대로 두고 faststart로 1회 무손실 리먹스**해 캐시 → 첫 재생이 빨라지고 이후엔 즉시. (이미 빠른 파일은 그대로 재생)
- HEVC를 재생 가능 코덱으로 인식(불필요한 재인코딩 방지).
- 0.7.31의 임시 변경 원복.

## v0.7.31 — 대용량 동영상 첫 로딩 속도 개선

### 수정
- 동영상 스트리밍에서 Range 없는 첫 요청에 파일 전체를 메모리로 읽던 것을 첫 청크(206)만 보내도록 변경. 대용량(수백 MB) 동영상의 첫 로딩 지연을 크게 줄임.

## v0.7.30 — 동영상 넘김 딜레이 수정

### 수정
- 크게 보기에서 좌우로 넘길 때 지나치는 비네이티브 동영상마다 변환이 시작돼 누적·지연되던 문제 수정. 잠깐 머무를 때(350ms 디바운스)만 변환을 시작하도록 변경.

## v0.7.29 — Pro Layout 겹침 수정

### 수정
- Pro Layout에서 우측 도구막대·다음 화살표가 사이드 패널과 겹치던 문제 수정(패널 왼쪽으로 이동).

## v0.7.28 — Pro Layout (Phase 2-2)

### 추가
- **Pro Layout**: 크게 보기에서 ▤ 버튼(또는 <kbd>Tab</kbd>)으로 오른쪽에 EXIF·별점·플래그 패널을 상시 도킹. 사진을 보면서 바로 별점/플래그를 매기는 빠른 셀렉트 레이아웃. 이미지 영역은 패널만큼 줄여 가려지지 않게 맞춤. 설정에 상태 저장.

## v0.7.27 — 정리 프리셋: 폴더별 복사·이동 동시 제공

### 변경
- 정리 프리셋의 동작(복사/이동)을 고정값이 아니라 **실행 시 선택**하도록 변경. 정리 ▾ 메뉴에서 각 폴더마다 "복사"·"이동" 버튼이 함께 보임. (설정에서는 폴더만 등록)

## v0.7.26 — 복사/이동 정리 프리셋 (Phase 2-1)

### 추가
- **정리 프리셋**: 설정 > 정리 프리셋에서 대상 폴더를 등록(복사/이동 선택)하고, 갤러리에서 사진을 선택한 뒤 선택바 **🗂 정리 ▾** 메뉴로 한 번에 복사·이동. 이동은 색인에서 제거(화면에서도 즉시 빠짐), 이름 충돌 시 자동으로 " (2)" 부여.

## v0.7.25 — 크게 보기 별점 해제(0) 수정

### 수정
- 크게 보기에서 <kbd>0</kbd> 키가 별점을 해제하지 않고 줌만 리셋하던 문제 수정(이제 <kbd>0</kbd> = 별점 해제, 줌 리셋은 더블클릭).

## v0.7.24 — 설명서 이미지 자리 + 문구 정리

### 변경
- 사용 설명서에 **스크린샷 자리(이미지 플레이스홀더)** 추가 — 각 위치에 캡처 범위·강조/화살표 가이드 표기. `images/`에 파일을 넣으면 안내 박스가 사라지고 이미지가 표시됨.
- 구현/기술 용어(웹뷰·코덱명 등) 노출 문구를 사용자 친화적 표현으로 정리.

## v0.7.23 — 단축키 시트 + 설정 검색 (Phase 1 마무리)

### 추가
- **단축키 시트**: `?` 키(또는 상단 ⌨ 버튼)로 전체 단축키 도움말 토글.
- **설정 검색**: 설정 상단 검색창으로 섹션/항목 즉시 필터.

## v0.7.22 — 필름스트립 사라짐 수정

### 수정
- **크게 보기 하단 필름스트립(같은 폴더 목록)이 사라지던 문제**: 자연 정렬 콜레이션 이름 `NATURAL`이 SQLite 예약어(NATURAL JOIN)라 `COLLATE NATURAL`이 구문 오류를 내 폴더 목록·이름 정렬 쿼리가 실패하던 것을 콜레이션명 `NATSORT`로 변경해 해결.

## v0.7.21 — 자연 정렬 + 줌 네비게이터

### 추가
- **자연 정렬**: 파일명 정렬을 사람이 기대하는 순서로(예: IMG_2 < IMG_10). 검색 정렬·필름스트립에 적용.
- **줌 네비게이터(미니맵)**: 크게 보기에서 확대하면 우하단에 전체 축소본 + 보이는 영역 표시. 클릭/드래그로 이동.

## v0.7.20 — 파일 이름 변경 + 정리

### 추가
- **파일 이름 변경**: 갤러리에서 사진/동영상 우클릭 → "✏ 이름 변경"(확장자 빼면 기존 확장자 유지, 충돌·금지문자 검사 + 색인 갱신).

### 정리
- 동영상 회전 진단용 임시 로그 제거.

## v0.7.19 — 사용 설명서(온·오프라인)

### 추가
- **사용 설명서 HTML**: 자체 완결형 가이드(ko/en)를 설치본에 번들(오프라인) + 소개 사이트에 게시(온라인). About에 "📖 사용 설명서" 버튼(번들 로컬 HTML 열기). 소개 페이지 푸터에도 링크.

## v0.7.18 — 최초 로딩 2단계화

### 수정
- **최초/재로드 시 첫 화면 우선 표시**: 처음에 첫 화면에 보일 만큼(약 120장)만 먼저 불러와 즉시 그리고, 그 다음 위아래 버퍼를 이어서 채우는 2단계 로딩으로 변경(첫 표시 체감 단축).

## v0.7.17 — 스크롤 멈춤 후 즉시 로딩

### 수정
- **스크롤을 멈춘 뒤 클릭/휠을 해야 로딩이 끝나던 체감 문제**: 스크롤이 멈추는 순간 현재 화면 범위를 디바운스 없이 즉시 적재하고, 프레임을 깨워 새로 보이는 셀의 렌더/디코드를 바로 반영하도록 수정.

## v0.7.16 — 과노출 경고 수정 + 비율 표시

### 수정
- **과노출(제브라) 빗금이 안 보이던 문제**: 원본 분석 캔버스가 CORS 미허용으로 오염되어 분석이 조용히 실패하던 것을 수정(photo:// 응답에 CORS 헤더 + 이미지 crossOrigin).

### 추가
- **과노출 비율 표시**: 크게 보기에서 날아간(클리핑) 픽셀 비율을 ▩ 버튼 옆에 %로 표시(과노출이 있을 때).

## v0.7.15 — 제작자 표기 변경

### 변경
- 제작/저작권 표기를 **ExaPeak Soft Solutions**로 변경(About·EULA·소개 페이지). 개인 이름 표기 제거.

## v0.7.14 — 설정 세그먼트 줄바꿈 수정

### 수정
- 설정의 세그먼트 버튼(낮음/보통/높음 등) 글자가 공간 부족 시 세로로 줄바꿈되던 문제 수정(버튼 줄바꿈 금지 + 폭 유지).

## v0.7.13 — 설정 정리 + 갤러리 스크롤 부하 조절

### 추가
- **갤러리 스크롤 설정**(설정 > 표시 > 갤러리 스크롤):
  - **미리 그리기 범위**(낮음/보통/높음): 화면 밖을 미리 그리는 양. 낮음일수록 빠른 스크롤 시 CPU/GPU 튐·끊김(다른 영상 재생 등)이 줄어듦.
  - **부드러운 스크롤 최적화**(켜짐/꺼짐): 화면 밖 썸네일 렌더 생략(content-visibility) + 비동기 디코딩으로 스크롤 부하 감소.
- **설정창 카테고리 그룹화**: 표시 / 라이브러리 / 프로그램 상위 그룹으로 섹션 정리.

## v0.7.12 — 동영상 재생 스마트 준비

### 수정
- **재생 중 변환본으로 교체(swap)되며 처음부터 다시 재생되던 문제 제거**: mp4 계열은 검사 없이 즉시 재생, mov/비네이티브는 변환 필요 여부를 먼저 확인(필요시 변환)한 뒤 한 번에 올바른 소스로 재생. 변환본이 이미 있으면 코덱 검사도 건너뛰어 반복 재생이 빠름.
  (mov가 영상은 되고 소리만 안 나는 경우 = 오디오 PCM → 소리를 위해 변환)

## v0.7.11 — 우클릭 폴더 열기 + 동영상 재시작 수정

### 추가
- **우클릭 메뉴**: 갤러리에서 사진/동영상을 우클릭하면 "폴더 열기 / 크게 보기 / 수정하기" 메뉴. 크게 보기 화면에서 우클릭하면 해당 파일 폴더를 탐색기에서 엶.

### 수정
- **동영상이 1~2초 후 처음부터 다시 재생되던 문제**: 변환본으로 자동 교체 시 재생 위치를 이어가도록 수정(0초로 리셋 안 함).

## v0.7.10 — 세로 동영상 잘림 확실히 수정

### 수정
- **세로 동영상 하단 잘림 재수정**: flex 높이 체인이 풀리지 않던 환경 대응. 영상 래퍼를 영역에 절대 채움(definite 크기)으로 바꾸고 영상을 object-fit:contain으로 표시해 잘림 제거.

## v0.7.9 — 동영상 재생 시작 속도 복구

### 수정
- **재생 로딩이 느려진 문제**: mov 수정(0.7.6) 이후 모든 동영상을 열 때 코덱 검사(ffprobe)가 재생 전에 끼어들어 시작이 느려짐. 네이티브 컨테이너는 즉시 원본을 재생하고, 변환 필요 여부(PCM 오디오 mov 등)는 백그라운드에서 확인해 필요할 때만 변환본으로 자동 교체하도록 변경.

## v0.7.8 — 세로 동영상 맞춤 수정

### 수정
- **세로(포트레이트) 동영상 하단이 잘리던 문제**: 영역 분리(0.7.7) 후 퍼센트 높이 체인이 풀리지 않아 세로 영상이 영역을 넘던 것을 수정. 영상을 영역 안에서 가로/세로 모두 맞춰 레터박스 표시.

## v0.7.7 — 크게 보기 레이아웃 분리

### 수정
- **동영상 컨트롤바가 하단 필름스트립에 가리던 문제**: 미디어 영역과 필름스트립을 flex 컬럼으로 물리적으로 분리(겹침 제거). 동영상이 항상 영역 안에 맞춰져 컨트롤바가 가려지지 않음.

## v0.7.6 — mov(PCM 오디오) 재생 수정

### 수정
- **mov 재생 안 됨**: 확장자만으로 "네이티브 재생" 판단하던 것을 **코덱 기준**으로 변경. mov 컨테이너여도 오디오가 PCM(올림푸스 등) 또는 비디오가 HEVC/ProRes면 WebView2가 못 풀므로 자동으로 재생용 mp4로 변환해 재생. (avi/m2ts/wmv 등 비네이티브는 기존대로 변환)

## v0.7.4 — 동영상 회전 지원 형식 확대

### 추가
- **3gp/3g2 무손실 회전 지원**: ISO 미디어 계열이라 mp4와 동일하게 무손실 회전 가능.
- **wmv 회전 지원(재인코딩)**: wmv(ASF)는 회전 메타데이터를 담지 못해 무손실이 불가 → 픽셀을 실제로 돌려 재인코딩(약간의 화질 손실·시간 소요). 상세 패널·크게 보기·갤러리 다중 회전 모두 적용.

## v0.7.3 — 동영상 회전 실제 적용 수정

### 수정
- **동영상 회전이 실제로 적용되지 않던 버그**: 휴대폰 영상 등 display matrix(회전 행렬)로 방향을 담는 파일은 구식 `rotate` 메타데이터 태그를 무시한다. 무손실 회전을 ffmpeg `-display_rotation`(표시 행렬 직접 설정)으로 변경해 실제로 회전이 반영되도록 수정. (상세 패널·크게 보기·갤러리 다중 회전 모두 적용)

## v0.7.2 — 동영상 회전 확장

### 추가
- **크게 보기 동영상 회전**: Lightbox에서도 동영상 회전(미리보기 → "적용", 무손실). mp4/mov/m4v 한정.
- **갤러리 다중 회전에 동영상 포함**: 선택바 ↻ 회전이 선택된 동영상(mp4/mov/m4v)도 함께 회전(무손실 메타데이터). 그 외 컨테이너는 건너뜀.

## v0.7.1 — 상세 패널 핀 유지

### 추가
- **고정핀 상태 영구화**: 상세 패널 고정 여부를 설정에 저장해 프로그램을 다시 실행해도 유지.

## v0.7.0 — Jobs 오류 관리

### 추가
- **Jobs 오류 폴더 열기**: 오류 항목별 "📂 폴더" 버튼으로 해당 파일을 탐색기에서 열고 선택.
- **오류 선택/전체 삭제**: 체크박스로 오류 로그를 선택해 삭제하거나 한 번에 비우기(파일은 그대로, 로그만 정리).

## v0.6.x — 비교·필터·타임라인

### 추가
- **사진 비교 보기**: 2~4장 선택 후 나란히 비교(4장=2×2). 줌/팬이 모든 칸에 동시 적용.
- **카메라/렌즈 다중선택 검색**: 콤보박스 안의 체크박스로 여러 기종 동시 선택.
- **카메라 컬러 라벨**: 다중 카메라 검색 시 셀 좌상단에 기종명(이름 기반 일관 색).
- **타임라인 날짜 범위**: 보이는 영역의 시작~끝 촬영일을 강조색 라벨로 표시.

### 수정
- Delete 키 확인 팝업 중복(닫힌 라이트박스가 키를 가로채던 문제) → 열림 가드.
- 삭제 후 클릭 시 목록이 갱신되던 문제 → 낙관적 제거 + 개수만 재동기화.
- 더블클릭 시 엉뚱한 사진 열림 → 셀을 사진 id로 추적(computeItemKey).
- 상세 패널 splitter 복귀 + 패널 열고 닫을 때 스크롤 한 줄 밀림 보정.
- 설정 진입 시 잠깐 멈춤 → 저장소 용량 계산을 백그라운드 스레드로.

## v0.5.x — 번들·정리

### 추가
- **FFmpeg 동봉**: ffmpeg/ffprobe(GPLv3)를 설치본에 포함(별도 실행 파일로 호출).
  설정 미지정 시 자동 사용. 라이선스 전문·고지(THIRD-PARTY-NOTICES) 동봉.
- **이용약관(EULA)**: 개인 비상업 무료, 상업/조직 사용은 별도 라이선스. 무보증.
- 상세(EXIF) 패널: 선택 없으면 숨김 + X로 닫기.
- m2ts 등 비호환 동영상: mp4로 자동 변환 후 재생(이미 4.x에 도입, 안정화).

## v0.4.9 — 2026-06

### 추가
- **소개(About) 모달**: 상단 ⓘ 버튼 — 버전·설명·소개 페이지 링크.
- **소개 웹페이지**(`site/`, 한국어/English)와 자동 배포 스크립트, 본 변경 이력 문서.

### 개선
- 라이트박스 **앞뒤 미리 불러오기 장수**를 설정값으로 분리(보기 > 미리 불러오기, 0~20).

## v0.4.x — 사진 고르기 워크플로

### 추가
- **별점(0~5)·플래그(채택/탈락)**: 그리드 배지, 상세·크게 보기에서 클릭/단축키
  (`1`~`5`, `0`, `P`/`X`/`U`), 다중 선택 일괄 적용, 검색 필터·별점 정렬.
- **타임라인**: 날짜 정렬 시 스크롤 위치의 촬영월을 떠다니는 라벨로 표시.
- **크게 보기 프리로드**: 이웃 원본을 미리 로드해 prev/next 즉시 표시.
- **m2ts 등 비호환 동영상 재생**: 열 때 재생용 mp4로 자동 변환(캐시) 후 재생.
- **크게 보기 Delete**: 확인 후 휴지통 삭제 → 다음 항목으로 이동.

### 수정
- 다중 삭제 후 로딩이 멈춰 있던 문제 → **낙관적 즉시 제거 + 위치 유지 정합**.
- 동영상 재생이 안 되던 문제(별점 바가 컨트롤을 가림) → 별점 바 위치 이동.
- 상세 패널이 외부 변경(회전·별점)에 즉시 반영되지 않던 문제.

## v0.3.x — 갤러리·검색 통합

### 변경
- **갤러리 = 검색 통합**: 별도 검색 화면 제거, 갤러리에서 🔎 검색 패널 토글.
- **네비 재배치**: 갤러리가 첫 화면, 라이브러리·작업·설정은 하단으로.
- 동작하지 않던 상단 검색창 제거.

### 추가
- **시작 시 자동 증분 스캔**(설정 on/off) — 닫힌 동안 바뀐 파일 자동 반영.

## v0.2.x — 뷰어·성능·정리 기반

### 추가
- **윈도우 가상화 / 무한 스크롤**: 수십만 장도 일정한 메모리로 매끄럽게.
- **자체 동영상 플레이어**(재생/탐색/볼륨/속도/전체화면), Range 스트리밍.
- **크게 보기**: 휠 줌·드래그 팬·EXIF 오버레이·폴더 필름스트립·이전/다음.
- **90° 회전**(원본 재저장, EXIF 보존) — 미리보기 즉시 회전.
- **외부 편집기 연동**("수정하기") — 사진/동영상 편집기 지정.
- **다중 선택**(드래그 러버밴드·Shift/Ctrl 범위) + **휴지통 삭제**(복구 가능).
- EXIF 검색(카메라·렌즈·ISO·조리개·초점거리·촬영일·형식), 콤보박스 정렬.

### 비고
- 한때 포함됐던 컬러 그레이딩/LUT 편집기는 외부 편집기 연동으로 대체되어 제거됨.

---

> 새 버전을 낼 때 이 파일 상단에 항목을 추가하고 `site/`도 함께 갱신하세요.
